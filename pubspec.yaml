name: audio_therapy_app
description: Personalized sound therapy generator

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.46
  path_provider: ^2.1.5
  file_picker: ^8.3.7
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true